Hacky PoC: `graph.ts` walked by Temporal in `temporal/`.

`npm i`, then `temporal server start-dev --db-filename temporal.db`, `npm run temporal:worker`, `npm run temporal`. `npm test` / `npm run typecheck`.

Effect samples and graph viz are in `effect-reference/`. HITL color-picker is in `temporal-reference/`.
